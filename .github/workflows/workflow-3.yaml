name: CI Pipeline
on: 
  workflow_dispatch:
env:
  ENVIRONMENT: production
jobs:
  Deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment
        if: ${{ env.ENVIRONMENT == 'production' && github.event_name == 'workflo_dispatch' }}
        run: echo "Deploying to Production"
