name: CI Pipeline
on: 
  workflow_dispatch:
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: exit 1
      - name: Show Error
        if: ${{ failure() }}
        run: echo "Tests failed"
