name: CI Pipeline
on: 
  workflow_dispatch:
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
    - name: Test
      run: echo "Running tests"
    - name: Cleanup
      if: ${{ always() }}
      run: echo "Cleaning temporary files"
