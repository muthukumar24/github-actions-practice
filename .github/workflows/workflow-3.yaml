name: CI Pipeline
on: 
  workflow_dispatch:
env:
  API_KEY: ${{ secrets.API_TOKEN }}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub - Secrets
        run: |
          echo "GitHub masks the secret if i try to print - $API_KEY"
