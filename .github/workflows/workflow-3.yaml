name: CI Pipeline
on: 
  workflow_dispatch:
env:
  API_KEY: ${{ secrets.API_TOKEN }}
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: GitHub - Secrets
        run: |
          echo "GitHub masks it - $API_KEY"
