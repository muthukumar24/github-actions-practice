name: CI Pipeline
on: 
  push:
    branches: [main]
  workflow_dispatch:
jobs:
  Build:
    if: ! (github.event_name == 'push')
    runs-on: ubuntu-latest
    steps:
      - run: echo "Application Build Completed"
