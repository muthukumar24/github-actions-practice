name: CI Pipeline
on: 
  workflow_dispatch:
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy Application
        run: echo "Deploying Application"
      - name: Cleanup After Cancellation
        if: ${{ cancelled() }}
        run: echo "Deployment was cancelled. Cleaning up..."
